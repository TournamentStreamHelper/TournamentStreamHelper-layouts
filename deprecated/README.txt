These layouts are no longer supported. They are kept so they can continue to be used if they are useful, or use as reference for future overlays.

You will need to move them to `TournamentStreamHelper/layout` (out of the `deprecated` folder)